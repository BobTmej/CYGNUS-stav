# CYGNUS-Stav

A simple status page for CYGNUS with admin panel, persistent storage and password protection via environment variables.

## Setup

```bash
npm install
node server.js
```

Frontend is available at `http://localhost:3000`.
